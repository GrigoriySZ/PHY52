var = 11
var2 = 15.3
# print(var + var2)
# print(var - var2)
# print(var * var2)
# print(var / var2)

# ctrl + / - быстрая команда для комментирования строки
# строкой считается набор символов в "" или в ''

name = "Гриша"
hello = 'Привет'
print(name)
# print(hello + " " + name) # конкотинация строк - склеивание несколькоих строчных
print(var % 2)  
# % (деление с остатк)ом - результатом операции будет остаток от целочисленного деления
# print(hello * 3)  # умножение строк дублирует строку n раз
print(var // 3)
# // (целочисленное деление) - результатом операции будет количество число с отбрасыванием дробной части

# b^2 - 4ac
var_a = int(input("Введите значение a"))
var_b = int(input("Введите значение b"))
var_c = int(input("Введите значение c"))
print(type(var_a))
# Функции преобразования типов
# int() -  преобразовывает тип данных в целое число int
# float() - преобразовывает тип данных в число с плавающей точкой float
# str() - преобразовывает тип даннысх в строчный str 
result = var_b * var_b - 4 * var_a * var_c
print(result)

# операторы отношений
# > - больше
# < - меньше
# == - равно(сравнить)
# != - не равно
# >= - больше или равно
# <= - меньше или равно

if result > 0: 
    print("Два корня")
if result < 0:
    print("Нет корней")
if result == 0:
    print("Один корень")
print("Программа завершина!")