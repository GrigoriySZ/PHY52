Мой первый гит репозиторий

Внесение изменений:
    Команды для GIT:
    git config --list
    git config user.name "Grigoriy"
    git config user.email "email_example@gmail.com"
    git add .
    git commit -m "initial readme-file"
    git status
    git log
    git log --graph
    git log --oneline
    git branch
    git branch [branch_name]
    git switch [branch_name]
    git checkout -b test2
    git merge [branch_name] -- слияение веток из ветки master